# 機能→Kuroco機能マッピング詳細

アプリの要件をKurocoの標準機能に対応付けるためのリファレンス。
**原則: カスタム実装を検討する前に、この表でKuroco標準機能に対応付けられないか確認する。**

## 目次

- [対応表（詳細）](#対応表詳細)
- [ヒアリング項目](#ヒアリング項目)
- [明細（1対多）の持ち方](#明細1対多の持ち方)
- [設計成果物テンプレート](#設計成果物テンプレート)
- [API接続状況表（フェーズ4-5の成果物）](#api接続状況表フェーズ4-5の成果物)
- [アプリ類型別のプリセット](#アプリ類型別のプリセット)

## 対応表（詳細）

| アプリの機能 | Kurocoの機能 | 作成・設定するもの | 実装の参照先 |
|-------------|-------------|------------------|-------------|
| データの一覧・詳細（記事、商品、レシピ等） | コンテンツ定義（TopicsGroup）+ Topics API | コンテンツ定義（拡張項目）、`Topics::list` / `Topics::details` エンドポイント | `/kuroco-content-structure`, `/kuroco-api-content` |
| 分類・絞り込み（カテゴリ） | カテゴリ（TopicsCategory） | コンテンツ定義内のカテゴリ設定、`filter` クエリ（`contents_type`） | `/kuroco-api-content`（フィルタークエリ） |
| 横断的なラベル付け | タグ | タグ設定、`filter` クエリ | 同上 |
| 選択肢マスタ（都道府県・ブランド等） | テーブルデータ（CSVテーブル） | CSVテーブル + コンテンツ定義の `csvtable` / `csvtable_checkbox` フィールド | `/kuroco-content-structure` |
| コンテンツ間の参照（関連記事・著者） | リレーション | `relation` フィールド（module: topics / member） | 同上 |
| 1件に複数明細（申請明細・商品バリエーション） | 繰り返しフィールドグループ / JSON項目 / 別コンテンツ定義＋リレーション | 想定行数で選ぶ（下記「明細（1対多）の持ち方」参照） | `/kuroco-content-structure` |
| 多段の申請・承認・差し戻し | 承認ワークフロー（Approvalflow） | ワークフロー設定（承認グループ＝フェーズ、承認者、メール通知）+ `Topics::waiting_for_approval_list` / `waiting_for_approval_details` / `accept` / `reject` エンドポイント。申請は `Topics::insert` / `update` に `approvalflow_id` を渡す | `/kuroco-docs`（`management/workflow`、`can-i-add-or-update-workflow-content-via-api`） |
| 業務工程のステータス管理（発注済み・検収完了 等） | `select` の状態項目 + `filter` | 状態項目（`searchable: true`）、一覧の絞り込みパラメータ | `/kuroco-api-content`（フィルタークエリ） |
| 変更履歴・監査ログ | コンテンツの変更履歴 / 独自のログ用コンテンツ定義 | `Topics::history_list` エンドポイント、または記録用コンテンツ定義＋トリガー処理 | `/kuroco-server-processing`（トリガー） |
| ログイン・ログアウト | 認証（Cookie or DynamicToken） | Authentication カテゴリのエンドポイント（`login_challenge`, `token`, `logout`, `profile`） | `/kuroco-api-content`（認証方式）, `/kuroco-frontend-integration`（実装） |
| 会員登録・マイページ | メンバー + メンバーグループ | `Member::insert` / `update` エンドポイント、拡張項目（会員属性） | 同上 |
| 会員限定コンテンツ | 閲覧制限（グループ制限） | コンテンツ定義・エンドポイントの閲覧制限設定 | `/kuroco-api-content`（閲覧制限の優先順序） |
| お気に入り・ブックマーク | Favorite | `Favorite::insert` / `Favorite::delete` / `Favorite::list` エンドポイント。人気順は Topics::list の `get_last_favorite_ymdhi` | `/kuroco-docs`（`favourites-feature` 等をGrep） |
| コメント | Comment（Activity） | Activity有効化 + `Comment::insert` / `Comment::list` エンドポイント | `/kuroco-docs`（`integrate-activity-comment`） |
| 問い合わせ・応募フォーム | フォーム（InquiryForm） | フォーム定義（項目・自動返信）+ `InquiryMessage::send` エンドポイント | `/kuroco-docs`（`setting-up-inquiry-forms`） |
| キーワード検索 | Topics検索 | `Topics::list` の `topics_keyword`。ベクトル検索は `vector_query` | `/kuroco-api-content` |
| ランキング・並び替え | ソート | `order_query` パラメータ（例: `ymd=DESC`） | 同上 |
| **集計・レポート・グラフ**（月次費用、件数推移、拠点別合計） | **標準機能なし。** Topics API に SUM / GROUP BY 相当の集計オペレーションはない（`group_by_category_id` はカテゴリ件数の集計専用） | 次の3択から選ぶ: ①対象レコードを `filter`（期間・拠点）で取得して**フロント側で合算**——**合算対象として取得するレコード数**（親か明細か、どちらを数えるかを明示する）が月あたり数百件までならこれで足りる。親に合計値を非正規化（`number` 項目）しておけば、明細が多くても親の件数だけ数えれば済む ②**バッチ処理で定期集計し、集計結果を専用コンテンツ定義に保存**——性能理由（件数が多い）とは別に、**業務理由（月末で締めた値を固定する・監査で後から変わってはいけない）でも②を選ぶ**。この2つは別の判断軸なので取り違えない ③カスタムSmartyエンドポイントで集計して返す。**明細を持つデータでは、この選択が「明細の持ち方」を左右する**（明細単位の絞り込み集計が要るなら明細を別コンテンツ定義にする） | `/kuroco-server-processing`（バッチ・Smarty）, `/kuroco-content-structure`（明細の持ち方） |
| 予約公開・公開期間 | 公開日時設定 | Topicsの公開設定（追加実装不要） | — |
| 多言語 | 多言語機能 | 言語設定 + `_lang` / `langs_open_flg` | `/kuroco-docs`（`building-a-multi-language-site`） |
| 通知（Slack・メール） | Webhook / トリガー / バッチ | トリガー設定 | `/kuroco-server-processing` |
| 決済・EC | EC機能（Stripe / Paygent） | EC設定一式 | `/kuroco-docs`（`subscription-billing-with-stripe`, `ec-*`） |
| 画像・ファイル添付 | 画像/ファイルフィールド + KurocoFiles | `image` / `file` フィールド | `/kuroco-content-structure` |
| 他システムとのデータ連携 | 直接呼び出し／プロキシ（`{api}`プラグイン）／取り込み（バッチ・スパイダー・Webhook） | 連携先ごとに異なる。判断基準はSKILL.md 0-4、深掘りは`/kuroco-server-processing`の「外部システム連携の方式設計」を参照 | `/kuroco-server-processing` |

## ヒアリング項目

フェーズ0で確認する質問（SKILL.md 0-1 の表の具体形。**番号は 0-1 の表と1対1で対応する**）。**列挙が必要な項目は自由記述を待たず、下の「アプリ類型別のプリセット」から叩き台を出して差分を直してもらう。**

1. **アプリの目的と業務範囲**: 何のためのアプリで、どこからどこまでを扱うか（例: 購買申請から請求書照合まで）
2. **アプリの種類**: ブログ・メディア / コーポレート / 会員制サイト / 一覧+詳細型アプリ（レシピ・ポートフォリオ・イベント等）/ 社内業務システム（申請・承認・工程管理） / EC / その他
3. **利用者の役割（ロール）**: 列挙してもらう（例: 申請者・部門長・購買担当・経理担当・管理者）。ロールごとに「見える範囲」「できる操作」が違う点まで確認する
4. **画面一覧**: 番号付きで列挙（トップ / 一覧 / 詳細 / 作成・編集 / 承認待ち / マスタ管理 / ダッシュボード / ログイン / マイページ / フォーム …）
5. **扱うデータの種類と親子関係**: 何を何種類扱うか。**1件に複数明細を持つデータがあるか、明細の項目と想定行数**（例: 申請1件に明細20行、明細は品名・数量・単価・税率）
6. **データごとの状態（ステータス）**: データ種別ごとに列挙。各状態について「誰が」「何をすると」「次はどこへ」進むか、差し戻し・取り下げの有無
7. **添付ファイル**: 種類と用途（例: 見積書・仕様書・契約書）、1件あたりの想定枚数
8. **必要な機能**: ログイン / 検索・絞り込み / お気に入り / コメント / 問い合わせ / グラフ・集計 / 変更履歴・監査ログ / 通知（メール・Slack）
9. **想定規模**: 利用者数、データ件数／年、同時利用の想定
10. **デバイス方針**: PC中心か。モバイルで使う画面はどれか（全画面か、特定画面だけか）
11. **デザイン方針と使うUIパターン**: トーン（例: 落ち着いた管理画面）と、使う部品の列挙（サイドバーナビ / ステータスバッジ / テーブル / 検索・フィルター / グラフ / タイムライン）
12. **デモ／サンプルデータの要件**: 投入するマスタとトランザクションの種類。**代表1件は具体値まで**（例: 新入社員8名分のPC・モニター購入申請、金額1,248,000円、2社の見積添付）
13. **他システムとの連携予定**: 連携先とデータの向き（取得のみ／送信のみ／双方向）。連携がある場合はパターン（直接呼び出し／プロキシ／取り込み）をSKILL.md 0-4で決める
14. **フレームワーク／公開方法**: 指定がなければ**アプリの類型名ではなくSKILL.md 1-1の判定条件**（クロールの要否／非エンジニアが再デプロイできるか）で選ぶ。公開方法は KurocoFrontへデプロイ / ローカル確認まで / 既存ホスティング

## 明細（1対多）の持ち方

5でヒアリングした想定行数で選ぶ。**判断は `/kuroco-content-structure`（Part 1 の 1. TopicsGroupの分割・2. JSON項目）に渡す**が、選択肢の性格は次のとおり。

| 持ち方 | 向くケース | 注意 |
|--------|-----------|------|
| 繰り返しフィールドグループ（`group_repetitions`） | 行数の上限が決まっていて、管理画面でも編集させたい | 上限はサイト設定に依存する（`whoami` の `site.limits.topics_ext_group_loop`）。行数を推測して設定しない |
| JSON項目 | 行数が可変・多い。明細単体での検索や絞り込みが不要 | 明細の項目でのfilter・並び替えができない |
| 別コンテンツ定義＋リレーション | 明細単体を検索・集計したい、明細に独自のステータスを持たせたい | 取得が2段になる（親と子で別エンドポイント、または `relation` フィールド経由） |

## 設計成果物テンプレート

フェーズ0の最後にこの形式でまとめ、ユーザーの合意を得る。**下の値は「社内業務システム（購買申請）」を例にした記入例で、書式を示すためのもの。自分の案件の語彙・認証方式・キャッシュ設定に置き換える**——とくに認証とキャッシュは案件で正反対になるので、例の値をそのまま持ち込まない。**該当しない表は「該当なし＋理由1行」で満たす**（空の表を出さない）。

```markdown
## 画面一覧
| 画面 | パス | 使用エンドポイント | 実装主体 | 認証 |
|------|------|------------------|---------|------|
| 申請一覧 | /requests | GET /rcms-api/{id}/requests, GET /rcms-api/{id}/requests/pending | フロント実装 | 必要 |
| 申請詳細 | /requests/[id] | GET /rcms-api/{id}/requests/{topics_id} | フロント実装 | 必要 |
| 申請作成 | /requests/new | POST /rcms-api/{id}/requests | フロント実装 | 必要 |
| 承認待ち一覧 | /approvals | GET /rcms-api/{id}/requests/pending, POST .../accept, POST .../reject | フロント実装 | 必要 |
| マスタ管理（部署・取引先） | — | — | Kuroco管理画面 | — |
| ログイン | /login | POST /rcms-api/{id}/login → POST /rcms-api/{id}/token | フロント実装 | — |

## コンテンツ定義
| 定義名 | フィールド | 型 | ext_slug | 必須 | searchable |
|--------|-----------|-----|---------|------|-----------|
| 購買申請 | 件名 | 標準 subject | — | ✓ | — |
| 購買申請 | 申請部署 | relation (topics) | dept | ✓ | ✓ |
| 購買申請 | 希望納期 | date | desired_date | ✓ | ✓ |
| 購買申請 | 工程ステータス | select | work_status | ✓ | ✓ |
| 購買申請 | 合計金額 | number | total | — | ✓ |
| 購買申請 | 申請明細（繰り返しグループ） | text / number | item_name / item_qty / item_price | — | — |

## エンドポイント
| パス | モデル::オペレーション | 認証 | キャッシュ |
|------|----------------------|------|-----------|
| login / token | Authentication::login_challenge / token | — | なし |
| requests | Topics::list | 必要 | なし |
| requests/{id} | Topics::details | 必要 | なし |
| requests（POST） | Topics::insert（approvalflow_id 付き） | 必要 | — |
| requests/pending | Topics::waiting_for_approval_list（get_workflow_info） | 必要 | なし |
| requests/{id}/accept, /reject | Topics::accept / Topics::reject | 必要 | — |

## ロール×画面の権限マトリクス
（○=可 / −=不可）

| 画面・操作 | 申請者 | 部門長 | 購買担当 | 管理者 | 条件 | 実現機構 |
|-----------|-------|-------|---------|-------|------|---------|
| 申請一覧（閲覧） | ○ | ○ | ○ | ○ | 申請者は自分の申請のみ／部門長は自部門のみ | 自分のみ=所有型（my_own_list）／自部門=属性型 |
| 申請作成 | ○ | ○ | − | ○ | — | 会員グループ（GroupAuth）＋エンドポイントの閲覧制限 |
| 承認・差し戻し | − | ○ | − | ○ | 自分の申請は承認不可 | 承認ワークフローの承認者指定＋「申請者自身の承認を許可する」を無効 |
| マスタ管理 | − | − | ○ | ○ | — | 会員グループ（管理画面） |

## ステータス遷移表（データ種別ごとに1表）
### 購買申請
| 現在の状態 | 遷移させるロール | 操作 | 次の状態 | 持ち方 |
|-----------|----------------|------|---------|-------|
| 下書き | 申請者 | 申請する | 申請中 | 承認ワークフロー（approvalflow_id付きでinsert） |
| 申請中 | 部門長 | 承認する | 承認済み | 承認ワークフロー（Topics::accept） |
| 申請中 | 部門長 | 差し戻す | 差し戻し | 承認ワークフロー（Topics::reject） |
| 承認済み | 購買担当 | 発注する | 発注済み | 状態項目（select） |
| 発注済み | 購買担当 | 検収登録 | 検収完了 | 状態項目（select） |

一覧のステータス表示（合成規則）: 申請一覧は Topics::list（承認済み以降）＋ Topics::waiting_for_approval_list（申請中）の2本を呼び、クライアント層でマージする。申請中データがあればフェーズ名（「部門長承認待ち」）、無ければ状態項目の値をバッジにする。

## データモデルの親子関係
| 親 | 子（明細） | 想定行数 | 持ち方 | 理由 |
|----|-----------|---------|--------|------|
| 購買申請 | 申請明細（品名・数量・単価・税率） | 1〜20行 | 繰り返しフィールドグループ | 上限が読めており、明細単体での絞り込み集計が不要。要るなら別定義＋リレーション |

## デモデータ仕様
| 種類 | 件数 | 内容 |
|------|------|------|
| マスタ（部署・取引先） | 各5〜10件 | — |
| ユーザー | ロールごとに1名以上 | 権限の出し分けを確認できるように |
| トランザクション | **各ステータス1件以上** | 一覧のバッジ・絞り込み・遷移ボタンの確認に必要 |
| 代表1件 | 1件 | ユーザー指定の具体値をそのまま使う |
```

### 記入時の注意（テンプレートには含めない）

- **条件を書くだけで終わらせず、「実現機構」列に何で実装するかを書く。** 「自分のものだけ」はパラメータ1つ、「自部門だけ」「自分に割り当てられた分だけ」はカスタム処理が必要になることもあり、**同じ見た目の1行でも工数が桁違い**。機構を書かないと、合意の時点でその差が見えない。型ごとの機構と使うパラメータは SKILL.md 0-3「レコードスコープの3つの型」が正。
- **上の例の「申請者自身の承認を許可する」を無効にする設定は、申請者と承認者が別人でいられることを前提にしている。** 承認者が1名しかいない／承認者も申請する運用では**有効にする**——無効のままだと承認者自身が出した申請を誰も承認できず恒久的に詰まる。前提が反転することを権限マトリクスの該当行に書く。
- **「同じグループのメンバーの投稿だけ見せる」は標準機能にない。** グループ制限・`my_own_list`／`self_only`・MemberCustomSearchAuth のどれで表現できるかを `/kuroco-auth-design` で確認し、**できないものはカスタム処理が必要な行として明示する**（要件を落とさず、工数を見えるようにする）。
- 画面一覧の「実装主体」列は、**書き手が少人数で管理画面を使える場合に投稿・編集画面をフロントに作らない選択**を可視化するためのもの（SKILL.md 0-3 成果物1参照）。**フロントに画面を作るか否かは画面単位の二択で、途中は無い。** 「フロント実装」の画面は読み取りも書き込み（登録・更新・削除・承認）も出力（CSV等）もフロントから実APIに接続する。**画面はあるが操作ボタンが disabled の状態は成果物として認めない。** 書き込みだけ管理画面に寄せるなら、上の例の「マスタ管理」のように操作の行を分けて「Kuroco管理画面」と書く。
- 画面一覧の「使用エンドポイント」列には、**その画面から呼ぶ書き込み（POST insert / update / delete / accept / reject）も一覧・詳細と同じ行に列挙する**。この列が SKILL.md 3-4（`api-export_openapi` との網羅照合）と 4-5（API接続状況表）の照合元になるので、読み取りだけ書いて書き込みを省くと、後続の照合が書き込みの欠落を検出できない。

## API接続状況表（フェーズ4-5の成果物）

SKILL.md 4-5 で提示する表の書式。**下の行は上の設計成果物テンプレートと同じ「社内業務システム（購買申請）」の記入例で、書式を示すためのもの。行はそのまま持ち込まず、自分の案件の画面一覧の「フロント実装」の行を画面 × 操作に展開して機械的に写す**（ここで行を減らさない）。「エンドポイント有無」は 3-4 の `api-export_openapi` との照合結果、「接続済み」は 4-1（読み取り）・4-2（書き込み・出力）で**実際に実行して確認した結果**だけを書く。記号は3種類: `○` 確認済み、`×` 未接続（または未作成）、`—` 対象外（0-3 で「作らない」と合意した行）。`×` と `—` を混ぜない——`×` は残っていたら完了報告できない行、`—` は合意済みで残してよい行。

```markdown
## API接続状況表
| 画面 | 操作 | 種別 | エンドポイント | エンドポイント有無（3-4） | 接続済み（4-1/4-2） | 確認方法・備考 |
|------|------|------|--------------|-----------------------|------------------|--------------|
| 申請一覧 | 一覧表示 | 読み取り | GET requests | ○ | ○ | 実データ5件が表示 |
| 申請一覧 | 承認待ちの合成表示 | 読み取り | GET requests/pending | ○ | ○ | 申請中1件がフェーズ名バッジで表示 |
| 申請作成 | 申請する | 書き込み | POST requests（approvalflow_id 付き） | ○ | ○ | 実行後 topics-list で申請中1件を確認 |
| 承認待ち一覧 | 承認する | 書き込み | POST requests/{id}/accept | ○ | ○ | 実行後 topics-get で状態項目が「承認済み」 |
| 承認待ち一覧 | 差し戻す | 書き込み | POST requests/{id}/reject | ○ | × | 未接続。再開条件: 差し戻し先の合意（確認事項 #3） |
| 申請一覧 | CSV出力 | 出力 | GET requests/export | ○ | ○ | 出力5件の金額・部署が topics-list の実データと一致 |
| マスタ管理 | 登録・更新 | 書き込み | — | 作らない（0-3 で管理画面と合意） | — | 執筆は管理画面で行う |
```

- 「接続済み」が `×` の行が1つでも残っていたら完了報告しない。残す行には「作らない」の合意（誰が・いつ）か、`[!]` の再開条件を備考に書く。
- 種別「出力」（CSV等）はレコードを残さないので、備考には「出力内容が Kuroco 側の実データと一致した」根拠（件数・照合した項目）を書く。画面に表示された・ダウンロードできた、だけでは `○` にしない。
- 「エンドポイント有無」が `×` で「作らない」の記録も無い行は、3-4 で未実装と判定して 3-1 に戻す（この表に残して先へ進まない）。

## アプリ類型別のプリセット

よくある依頼はこの構成から出発し、ヒアリングで差分を詰める:

### ブログ・メディア
- コンテンツ定義: 記事（画像 / 本文 / カテゴリ / タグ / 関連記事）
- エンドポイント: `list`（カテゴリ・タグfilter、キーワード検索付き）, `details`
- 画面: トップ（新着）、一覧（絞り込み）、詳細、検索

### コーポレートサイト
- コンテンツ定義: お知らせ、実績、（固定ページはフロント側 or ページ用定義）
- エンドポイント: `list` / `details` ×定義数、`InquiryMessage::send`（問い合わせ）
- 画面: トップ、お知らせ一覧・詳細、会社概要（静的）、問い合わせフォーム

### 社内業務システム（申請・承認・工程管理型）
- ロール: 申請者 / 承認者（部門長等） / 実務担当（購買・経理等） / 管理者。**ロールごとに「見える範囲」が違うので、ロール列挙と同時にレコード単位の制限も聞く**
- コンテンツ定義: 申請（+明細）、マスタ（部署・取引先・予算）、（必要なら）ログ
- 状態: 承認までは承認ワークフロー、承認後の工程は `select` の状態項目（`searchable: true`）で持つ
- エンドポイント: `list` / `details` / `insert` / `update`、`waiting_for_approval_list` / `accept` / `reject`、`login` / `profile`、マスタの `list`
- 画面: ダッシュボード、申請一覧（絞り込み）、申請作成、申請詳細（承認タイムライン）、承認待ち一覧、工程別の管理画面、マスタ管理、（必要なら）監査ログ
- 認証: ログイン必須。会員グループ設計とレコード単位の制限は `/kuroco-auth-design`

### 会員制・アクション付きアプリ（レシピ・イベント等）
- コンテンツ定義: メインコンテンツ（+マスタ用テーブルデータ）
- メンバー: 会員グループ、認証（フロントとAPIを同一親ドメインに揃えられるならCookie、クロスドメインSPAは動的アクセストークン。判断基準は `/kuroco-frontend-integration` のサードパーティCookie問題を参照）
- エンドポイント: `list` / `details`, `login` / `logout` / `profile`, `Member::insert`, `Favorite::*`, （必要なら）`Comment::*`
- 画面: 一覧・詳細、ログイン・会員登録、マイページ（お気に入り一覧）
